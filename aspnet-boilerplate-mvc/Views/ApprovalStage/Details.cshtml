@model aspnet_boilerplate_mvc.Entities.ApprovalStage

@{
    ViewData["Title"] = "Approval Stage Details";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Home</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "ApprovalStage")">Approval Stages</a></li>
        <li class="breadcrumb-item active" aria-current="page">Details</li>
    </ol>
</nav>

<h2>Approval Stage Details</h2>

<div>
    <h4>Approval Stage Details</h4>
    <dl class="dl-horizontal">
        <dt>ID:</dt>
        <dd>@Model.Id</dd>
        <dt>Name:</dt>
        <dd>@Model.Name</dd>
        <dt>Code:</dt>
        <dd>@Model.Code</dd>
    </dl>
</div>